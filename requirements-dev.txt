# Dev/test dependencies (not shipped in the runtime image).
# Stage these in a wheelhouse for airgapped CI, same as app/requirements.txt.
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
ruff==0.9.2
