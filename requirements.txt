# Kiro-API V3 — runtime dependencies (core service).
# Install:  pip install -r requirements.txt
# Or install the package (which reads pyproject.toml):  pip install -e .
#
# Optional extras (not required to run the service):
#   Tray companion:  pip install "pystray>=0.19" "Pillow>=10.0" "requests>=2.31"
#   Development:     pip install "pytest>=8.0" "pytest-asyncio>=0.23" "httpx>=0.27" "ruff>=0.5"

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
